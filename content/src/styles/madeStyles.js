import s from './styles.scss'

export const dialogStyles = {
  root: s.dialogRoot,
  paper: s.dialogPaper,
}
export const dialogActionsStyles = {
  root: s.dialogActionsRoot,
}
export const toastStyles = {
  root: s.toastRoot,
}
export const alertStyles = {
  root: s.alertRoot,
  icon: s.alertIcon,
  message: s.alertMessage,
}
